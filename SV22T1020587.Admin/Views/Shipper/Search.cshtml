@model SV22T1020587.Models.Common.PagedResult<SV22T1020587.Models.Partner.Shipper>
@{
    Layout = null; // Bắt buộc phải có để không bị lặp lại Layout chính
}

<div class="mb-2 text-muted">
    Có tất cả <strong>@Model.RowCount</strong> người giao hàng trong <strong>@Model.PageCount</strong> trang
</div>

<div class="card">
    <div class="card-body p-1">
        <div class="table-responsive">
            <table class="table table-bordered table-hover mb-0">
                <thead class="table-secondary">
                    <tr>
                        <th>Tên người giao hàng</th>
                        <th>Điện thoại</th>
                        <th style="width:100px" class="text-center">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.DataItems != null)
                    {
                        foreach (var item in Model.DataItems)
                        {
                            <tr>
                                <td class="align-middle">@item.ShipperName</td>
                                <td class="align-middle">@item.Phone</td>
                                <td class="text-center text-nowrap">
                                    <a href="~/Shipper/Edit/@item.ShipperID" class="btn btn-sm btn-primary">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <a href="~/Shipper/Delete/@item.ShipperID" class="btn btn-sm btn-danger">
                                        <i class="bi bi-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>

    @if (Model.PageCount > 1)
    {
        <div class="card-footer">
            <ul class="pagination justify-content-center mb-0">
                @for (int p = 1; p <= Model.PageCount; p++)
                {
                    if (p == Model.Page)
                    {
                        <li class="page-item active"><a class="page-link" href="javascript:;">@p</a></li>
                    }
                    else
                    {
                        <li class="page-item">
                            <a class="page-link" href="javascript:;" onclick="doSearch(@p)">@p</a>
                        </li>
                    }
                }
            </ul>
        </div>
    }
</div>